apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: backend
  labels:
    name: backend
spec:
  rules:
    - host: api-lol-ensai-2026.kub.sspcloud.fr # mettre un truc unique 
      http:
        paths:
          - pathType: Prefix
            path: "/"
            backend:
              service:
                name: backend
                port:
                  number: 80