version: 1
formatters:
  simple:
    format: '%(asctime)s - %(levelname)-8s - %(message)s'
    datefmt: "%d/%m/%Y %H:%M:%S"
handlers:
  front_file:
    class: logging.FileHandler
    formatter: simple
    filename: logs/frontend.log
    mode : w
    encoding: utf8
loggers:
  frontLogger:
    handlers: [front_file]
    propagate: no
root:
  level: INFO
  handlers: [front_file]